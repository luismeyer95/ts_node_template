{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "npm start",
            "preLaunchTask": "npm: build",
            "name": "Run npm start",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}
